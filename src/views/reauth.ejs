<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Account Link Expired</title>
    <style>
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        font-family: Arial, sans-serif;
        background-color: #ffebee;
      }

      .container {
        text-align: center;
        padding: 2rem;
        background: #ffffff;
        border-radius: 10px;
        box-shadow: 0 4px 12px rgba(255, 0, 0, 0.2);
        max-width: 500px;
        width: 90%;
      }

      .error-icon {
        font-size: 4rem;
        color: #d32f2f;
        margin-bottom: 1rem;
      }

      h1 {
        color: #c62828;
        margin-bottom: 1rem;
      }

      p {
        color: #b71c1c;
        margin-bottom: 2rem;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="error-icon">❌</div>
      <h1>Link Expired or Invalid</h1>
      <p>
        The link you tried to access has expired, already been used, or is
        invalid. Please restart the process.
      </p>
    </div>
  </body>
</html>
